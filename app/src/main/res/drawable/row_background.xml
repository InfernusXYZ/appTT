<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Fondo de la fila -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray" /> <!-- Borde de la tabla -->
    <corners android:radius="0dp" /> <!-- Sin esquinas redondeadas -->
</shape>
